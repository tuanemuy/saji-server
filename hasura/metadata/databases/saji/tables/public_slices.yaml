table:
  name: slices
  schema: public
array_relationships:
  - name: revisions
    using:
      foreign_key_constraint_on:
        column: slice_id
        table:
          name: revisions
          schema: public
  - name: tags
    using:
      foreign_key_constraint_on:
        column: slice_id
        table:
          name: slices_tags
          schema: public
